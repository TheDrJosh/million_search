services:
    backend:
        build: 
            context: .
            dockerfile: ./million_backend/Dockerfile
        ports:
            - "8080:8080"
        links:
          - "database:database"
        develop:
          watch:
            - action: rebuild
              path: ./million_backend

    database:
        image: "postgres:16.2"
        restart: always
        shm_size: 128mb
        environment:
            - POSTGRES_PASSWORD=password1234
